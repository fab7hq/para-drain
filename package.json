{
  "name": "paradrain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ParaDrain 0.1 \u2014 twin-basket drain filter, editable Blender model and interactive demonstration",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "python3 tools/clean_png_metadata.py && node tools/build_docs.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "npm run build && playwright test",
    "package": "npm test && npm run test:geometry && npm run build && python3 tools/package_release.py",
    "test:geometry": "python3 tools/validate_geometry.py",
    "preview:pages": "wrangler pages dev dist --ip 127.0.0.1 --port 4181",
    "deploy:pages": "npm run build && wrangler pages deploy dist --project-name paradrain --branch main"
  },
  "dependencies": {
    "three": "0.186.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "marked": "17.0.4",
    "vite": "8.3.0",
    "wrangler": "4.131.1"
  },
  "homepage": "https://paradrain.getfab7.com",
  "engines": {
    "node": ">=22.12.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fab7hq/para-drain.git"
  }
}
